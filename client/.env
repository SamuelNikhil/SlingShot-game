# Game Server Configuration (Your EC2 instance running Geckos.io)
VITE_SERVER_URL=http://13.127.217.1
VITE_SERVER_PORT=3000

# Connection Mode: 'true' = use hosting proxy, 'false' = direct connection (faster)
VITE_USE_PROXY=false

# Signaling Path (only used when VITE_USE_PROXY=true)
# VITE_SIGNALING_PATH=/.wrtc