# Production Environment for Hostinger
# ======================================
# Copy this to .env before building for Hostinger deployment

# Your game server URL (could be a VPS, Render, etc.)
VITE_SERVER_URL=https://your-game-server-domain.com
VITE_SERVER_PORT=3000

# Direct connection mode (recommended for best latency)
VITE_USE_PROXY=false

# If you need to use proxy mode (e.g., game server not directly accessible):
# VITE_USE_PROXY=true
# VITE_SIGNALING_PATH=/.wrtc
